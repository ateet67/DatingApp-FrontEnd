import { AlphNumericDirective } from './alph-numeric.directive';

describe('AlphNumericDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphNumericDirective();
    expect(directive).toBeTruthy();
  });
});
